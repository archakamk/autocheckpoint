# AutoCheckpoint

Automatic memory optimization for PyTorch training - enabling 2-3x larger models through intelligent gradient checkpointing.

## Status

🚧 Under active development

## What is this?

A tool that automatically figures out the optimal gradient checkpointing strategy for PyTorch models to reduce memory usage during training.